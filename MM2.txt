local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ScreenGui oluştur
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = playerGui

-- ImageLabel oluştur
local imageLabel = Instance.new("ImageLabel")
imageLabel.Parent = screenGui
imageLabel.Size = UDim2.new(0, 300, 0, 300)
imageLabel.Position = UDim2.new(0.5, -150, -0.3, 0) -- yukarıdan başlasın
imageLabel.BackgroundTransparency = 0
imageLabel.Image = "rbxassetid://134803730453170"
imageLabel.ImageTransparency = 0

-- Aşağı inme animasyonu
local appearTween = TweenService:Create(
	imageLabel,
	TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	{Position = UDim2.new(0.5, -150, 0.5, -150)}
)

appearTween:Play()
appearTween.Completed:Wait()


local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Tema
local Theme = {
	Background = Color3.fromRGB(20, 25, 40),
	Sidebar = Color3.fromRGB(15, 20, 35),
	Accent = Color3.fromRGB(80, 140, 255),
	Text = Color3.fromRGB(230, 235, 255),
	TextDim = Color3.fromRGB(160, 170, 200),
	ToggleOn = Color3.fromRGB(80, 140, 255),
	ToggleOff = Color3.fromRGB(50, 55, 75),
	Stroke = Color3.fromRGB(40, 50, 80),
	Element = Color3.fromRGB(25, 30, 48)
}

-- UI Library
local UILibrary = {}
UILibrary.__index = UILibrary

function UILibrary.new(title)
	local self = setmetatable({}, UILibrary)

	self.ScreenGui = Instance.new("ScreenGui")
	self.ScreenGui.Name = "2btrHub"
	self.ScreenGui.ResetOnSpawn = false
	self.ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	self.ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

	-- Ana Frame
	self.Main = Instance.new("Frame")
	self.Main.Name = "Main"
	self.Main.Size = UDim2.new(0, 520, 0, 380)
	self.Main.Position = UDim2.new(0.5, -260, 0.5, -190)
	self.Main.BackgroundColor3 = Theme.Background
	self.Main.BorderSizePixel = 0
	self.Main.ClipsDescendants = true
	self.Main.Parent = self.ScreenGui

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 12)
	corner.Parent = self.Main

	local stroke = Instance.new("UIStroke")
	stroke.Color = Theme.Stroke
	stroke.Thickness = 1
	stroke.Parent = self.Main

	-- Arka plan resmi
	local backgroundImage = Instance.new("ImageLabel")
	backgroundImage.Name = "BackgroundImage"
	backgroundImage.Size = UDim2.new(1, 0, 1, 0)
	backgroundImage.BackgroundTransparency = 1
	backgroundImage.Image = "rbxassetid://140222022471910"
	backgroundImage.ImageTransparency = 0.65
	backgroundImage.ScaleType = Enum.ScaleType.Crop
	backgroundImage.ZIndex = 0
	backgroundImage.Parent = self.Main

	local imageCorner = Instance.new("UICorner")
	imageCorner.CornerRadius = UDim.new(0, 12)
	imageCorner.Parent = backgroundImage

	-- Üst bar (sürükleme buradan yapılacak)
	local topBar = Instance.new("Frame")
	topBar.Name = "TopBar"
	topBar.Size = UDim2.new(1, 0, 0, 32)
	topBar.BackgroundTransparency = 1
	topBar.ZIndex = 10
	topBar.Parent = self.Main

	-- Minimize (-) butonu
	local minimizeBtn = Instance.new("TextButton")
	minimizeBtn.Name = "Minimize"
	minimizeBtn.Size = UDim2.new(0, 32, 0, 32)
	minimizeBtn.Position = UDim2.new(1, -70, 0, 0)
	minimizeBtn.BackgroundTransparency = 1
	minimizeBtn.Text = "—"
	minimizeBtn.TextColor3 = Theme.TextDim
	minimizeBtn.Font = Enum.Font.GothamBold
	minimizeBtn.TextSize = 18
	minimizeBtn.ZIndex = 11
	minimizeBtn.Parent = topBar

	minimizeBtn.MouseEnter:Connect(function()
		minimizeBtn.TextColor3 = Theme.Text
	end)
	minimizeBtn.MouseLeave:Connect(function()
		minimizeBtn.TextColor3 = Theme.TextDim
	end)

	-- Close (X) butonu
	local closeBtn = Instance.new("TextButton")
	closeBtn.Name = "Close"
	closeBtn.Size = UDim2.new(0, 32, 0, 32)
	closeBtn.Position = UDim2.new(1, -36, 0, 0)
	closeBtn.BackgroundTransparency = 1
	closeBtn.Text = "✕"
	closeBtn.TextColor3 = Theme.TextDim
	closeBtn.Font = Enum.Font.GothamBold
	closeBtn.TextSize = 16
	closeBtn.ZIndex = 11
	closeBtn.Parent = topBar

	closeBtn.MouseEnter:Connect(function()
		closeBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
	end)
	closeBtn.MouseLeave:Connect(function()
		closeBtn.TextColor3 = Theme.TextDim
	end)

	closeBtn.MouseButton1Click:Connect(function()
		self.ScreenGui:Destroy()
	end)

	-- ======================
	-- FLOATING LOGO BUTTON
	-- ======================
	local clickGUIButton = Instance.new("ImageButton")
	clickGUIButton.Name = "ClickGUIButton"
	clickGUIButton.Size = UDim2.new(0, 70, 0, 70)
	clickGUIButton.Position = UDim2.new(0.7, 0, 0.15, 0)
	clickGUIButton.Image = "rbxassetid://134803730453170"
	clickGUIButton.BackgroundTransparency = 1
	clickGUIButton.Visible = false
	clickGUIButton.ZIndex = 20
	clickGUIButton.Parent = self.ScreenGui

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 12)
	btnCorner.Parent = clickGUIButton

	local aspect = Instance.new("UIAspectRatioConstraint")
	aspect.AspectRatio = 1
	aspect.Parent = clickGUIButton

	-- Draggable logo
	local draggingLogo = false
	local dragInputLogo, dragStartLogo, startPosLogo

	local function updateLogo(input)
		local delta = input.Position - dragStartLogo
		clickGUIButton.Position = UDim2.new(
			startPosLogo.X.Scale,
			startPosLogo.X.Offset + delta.X,
			startPosLogo.Y.Scale,
			startPosLogo.Y.Offset + delta.Y
		)
	end

	clickGUIButton.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			draggingLogo = true
			dragStartLogo = input.Position
			startPosLogo = clickGUIButton.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					draggingLogo = false
				end
			end)
		end
	end)

	clickGUIButton.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement then
			dragInputLogo = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInputLogo and draggingLogo then
			updateLogo(input)
		end
	end)

	-- Minimize
	minimizeBtn.MouseButton1Click:Connect(function()
		self.Main.Visible = false
		clickGUIButton.Visible = true
	end)

	-- Logo tıklanınca UI açılsın
	clickGUIButton.MouseButton1Click:Connect(function()
		if draggingLogo then return end
		self.Main.Visible = true
		clickGUIButton.Visible = false
	end)

	-- Sidebar
	self.Sidebar = Instance.new("Frame")
	self.Sidebar.Name = "Sidebar"
	self.Sidebar.Size = UDim2.new(0, 140, 1, 0)
	self.Sidebar.BackgroundColor3 = Theme.Sidebar
	self.Sidebar.BackgroundTransparency = 0.15
	self.Sidebar.BorderSizePixel = 0
	self.Sidebar.ZIndex = 1
	self.Sidebar.Parent = self.Main

	local sideCorner = Instance.new("UICorner")
	sideCorner.CornerRadius = UDim.new(0, 12)
	sideCorner.Parent = self.Sidebar

	-- Başlık
	local titleLabel = Instance.new("TextLabel")
	titleLabel.Size = UDim2.new(1, -20, 0, 40)
	titleLabel.Position = UDim2.new(0, 10, 0, 10)
	titleLabel.BackgroundTransparency = 1
	titleLabel.Text = title or "UI Library"
	titleLabel.TextColor3 = Theme.Text
	titleLabel.Font = Enum.Font.GothamBold
	titleLabel.TextSize = 15
	titleLabel.TextXAlignment = Enum.TextXAlignment.Left
	titleLabel.ZIndex = 2
	titleLabel.Parent = self.Sidebar

	-- Tab Listesi
	self.TabList = Instance.new("ScrollingFrame")
	self.TabList.Name = "TabList"
	self.TabList.Size = UDim2.new(1, -10, 1, -60)
	self.TabList.Position = UDim2.new(0, 5, 0, 55)
	self.TabList.BackgroundTransparency = 1
	self.TabList.ScrollBarThickness = 3
	self.TabList.ScrollBarImageColor3 = Theme.Accent
	self.TabList.CanvasSize = UDim2.new(0, 0, 0, 0)
	self.TabList.ZIndex = 2
	self.TabList.Parent = self.Sidebar

	local listLayout = Instance.new("UIListLayout")
	listLayout.Padding = UDim.new(0, 4)
	listLayout.Parent = self.TabList
	self.ListLayout = listLayout

	-- Content
	self.Content = Instance.new("Frame")
	self.Content.Name = "Content"
	self.Content.Size = UDim2.new(1, -150, 1, -20)
	self.Content.Position = UDim2.new(0, 145, 0, 10)
	self.Content.BackgroundTransparency = 1
	self.Content.ZIndex = 1
	self.Content.Parent = self.Main

	self.Tabs = {}
	self.CurrentTab = nil

	-- Sürükleme SADECE topBar'dan
	self:MakeDraggable(self.Main, topBar)

	return self
end

function UILibrary:MakeDraggable(frame, dragHandle)
	local dragging = false
	local dragInput, dragStart, startPos

	dragHandle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	dragHandle.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement then
			dragInput = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(
				startPos.X.Scale,
				startPos.X.Offset + delta.X,
				startPos.Y.Scale,
				startPos.Y.Offset + delta.Y
			)
		end
	end)
end

function UILibrary:CreateTab(name, icon)
	local tabButton = Instance.new("TextButton")
	tabButton.Size = UDim2.new(1, -10, 0, 32)
	tabButton.BackgroundColor3 = Color3.fromRGB(25, 30, 50)
	tabButton.BackgroundTransparency = 1
	tabButton.Text = "  " .. (icon or "•") .. "  " .. name
	tabButton.TextColor3 = Theme.TextDim
	tabButton.Font = Enum.Font.Gotham
	tabButton.TextSize = 13
	tabButton.TextXAlignment = Enum.TextXAlignment.Left
	tabButton.AutoButtonColor = false
	tabButton.ZIndex = 3
	tabButton.Parent = self.TabList

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = tabButton

	local tabContent = Instance.new("ScrollingFrame")
	tabContent.Name = name
	tabContent.Size = UDim2.new(1, 0, 1, 0)
	tabContent.BackgroundTransparency = 1
	tabContent.Visible = false
	tabContent.ScrollBarThickness = 4
	tabContent.ScrollBarImageColor3 = Theme.Accent
	tabContent.CanvasSize = UDim2.new(0, 0, 0, 0)
	tabContent.ZIndex = 2
	tabContent.Parent = self.Content

	local contentLayout = Instance.new("UIListLayout")
	contentLayout.Padding = UDim.new(0, 8)
	contentLayout.Parent = tabContent

	local contentPadding = Instance.new("UIPadding")
	contentPadding.PaddingLeft = UDim.new(0, 8)
	contentPadding.PaddingRight = UDim.new(0, 8)
	contentPadding.PaddingTop = UDim.new(0, 5)
	contentPadding.Parent = tabContent

	local tab = {
		Button = tabButton,
		Content = tabContent,
		Name = name
	}

	table.insert(self.Tabs, tab)

	contentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		tabContent.CanvasSize = UDim2.new(0, 0, 0, contentLayout.AbsoluteContentSize.Y + 15)
	end)

	self.ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		self.TabList.CanvasSize = UDim2.new(0, 0, 0, self.ListLayout.AbsoluteContentSize.Y + 10)
	end)

	tabButton.MouseButton1Click:Connect(function()
		self:SelectTab(tab)
	end)

	if #self.Tabs == 1 then
		self:SelectTab(tab)
	end

	return tab
end

function UILibrary:SelectTab(tab)
	for _, t in pairs(self.Tabs) do
		t.Content.Visible = false
		t.Button.BackgroundTransparency = 1
		t.Button.TextColor3 = Theme.TextDim
	end

	tab.Content.Visible = true
	tab.Button.BackgroundTransparency = 0.6
	tab.Button.TextColor3 = Theme.Text
	self.CurrentTab = tab
end

function UILibrary:CreateSection(tab, text)
	local section = Instance.new("TextLabel")
	section.Size = UDim2.new(1, 0, 0, 24)
	section.BackgroundTransparency = 1
	section.Text = text
	section.TextColor3 = Theme.Accent
	section.Font = Enum.Font.GothamBold
	section.TextSize = 14
	section.TextXAlignment = Enum.TextXAlignment.Left
	section.ZIndex = 3
	section.Parent = tab.Content
end

function UILibrary:CreateToggle(tab, text, default, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 36)
	container.BackgroundColor3 = Theme.Element
	container.BackgroundTransparency = 0.25
	container.BorderSizePixel = 0
	container.ZIndex = 3
	container.Parent = tab.Content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -60, 1, 0)
	label.Position = UDim2.new(0, 12, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Theme.Text
	label.Font = Enum.Font.Gotham
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 4
	label.Parent = container

	local toggle = Instance.new("TextButton")
	toggle.Size = UDim2.new(0, 42, 0, 22)
	toggle.Position = UDim2.new(1, -52, 0.5, -11)
	toggle.BackgroundColor3 = default and Theme.ToggleOn or Theme.ToggleOff
	toggle.Text = ""
	toggle.AutoButtonColor = false
	toggle.ZIndex = 4
	toggle.Parent = container

	local toggleCorner = Instance.new("UICorner")
	toggleCorner.CornerRadius = UDim.new(1, 0)
	toggleCorner.Parent = toggle

	local circle = Instance.new("Frame")
	circle.Size = UDim2.new(0, 16, 0, 16)
	circle.Position = default and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
	circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	circle.BorderSizePixel = 0
	circle.ZIndex = 5
	circle.Parent = toggle

	local circleCorner = Instance.new("UICorner")
	circleCorner.CornerRadius = UDim.new(1, 0)
	circleCorner.Parent = circle

	local state = default or false

	toggle.MouseButton1Click:Connect(function()
		state = not state
		local tweenInfo = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

		TweenService:Create(toggle, tweenInfo, {
			BackgroundColor3 = state and Theme.ToggleOn or Theme.ToggleOff
		}):Play()

		TweenService:Create(circle, tweenInfo, {
			Position = state and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
		}):Play()

		if callback then
			callback(state)
		end
	end)

	return {
		Set = function(value)
			state = value
			toggle.BackgroundColor3 = state and Theme.ToggleOn or Theme.ToggleOff
			circle.Position = state and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
		end,
		Get = function()
			return state
		end
	}
end


function UILibrary:CreateDropdown(tab, text, options, default, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 36)
	container.BackgroundColor3 = Theme.Element
	container.BackgroundTransparency = 0.25
	container.BorderSizePixel = 0
	container.ZIndex = 5 -- Daha yüksek ZIndex
	container.ClipsDescendants = false -- Menünün dışarı taşmasına izin ver
	container.Parent = tab.Content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.45, 0, 1, 0)
	label.Position = UDim2.new(0, 12, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Theme.Text
	label.Font = Enum.Font.Gotham
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 6
	label.Parent = container

	local selected = default or (options[1] or "None")

	local dropButton = Instance.new("TextButton")
	dropButton.Size = UDim2.new(0.48, 0, 0, 26)
	dropButton.Position = UDim2.new(0.50, 0, 0.5, -13)
	dropButton.BackgroundColor3 = Color3.fromRGB(35, 40, 60)
	dropButton.Text = selected
	dropButton.TextColor3 = Theme.Text
	dropButton.Font = Enum.Font.Gotham
	dropButton.TextSize = 12
	dropButton.ZIndex = 6
	dropButton.Parent = container

	local dropCorner = Instance.new("UICorner")
	dropCorner.CornerRadius = UDim.new(0, 6)
	dropCorner.Parent = dropButton

	-- Seçenekler için ScrollingFrame (Çok oyuncu varsa kaydırmak için)
	local dropFrame = Instance.new("ScrollingFrame")
	dropFrame.Name = "DropFrame"
	dropFrame.Size = UDim2.new(0.48, 0, 0, 0)
	dropFrame.Position = UDim2.new(0.50, 0, 1, 4)
	dropFrame.BackgroundColor3 = Color3.fromRGB(30, 35, 55)
	dropFrame.Visible = false
	dropFrame.ZIndex = 100 -- En üstte görünmesi için
	dropFrame.ScrollBarThickness = 3
	dropFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
	dropFrame.BorderSizePixel = 0
	dropFrame.Parent = container

	local dropFrameCorner = Instance.new("UICorner")
	dropFrameCorner.CornerRadius = UDim.new(0, 6)
	dropFrameCorner.Parent = dropFrame

	local listLayout = Instance.new("UIListLayout")
	listLayout.Parent = dropFrame
	listLayout.SortOrder = Enum.SortOrder.LayoutOrder

	local isOpen = false

	local function RefreshOptions(newOptions)
		-- Eski seçenekleri temizle
		for _, child in pairs(dropFrame:GetChildren()) do
			if child:IsA("TextButton") then
				child:Destroy()
			end
		end

		for _, option in pairs(newOptions) do
			local optBtn = Instance.new("TextButton")
			optBtn.Size = UDim2.new(1, 0, 0, 26)
			optBtn.BackgroundColor3 = Color3.fromRGB(30, 35, 55)
			optBtn.BackgroundTransparency = 0 -- Tam görünür yapıldı
			optBtn.BorderSizePixel = 0
			optBtn.Text = option
			optBtn.TextColor3 = Theme.Text
			optBtn.Font = Enum.Font.Gotham
			optBtn.TextSize = 12
			optBtn.ZIndex = 101
			optBtn.Parent = dropFrame

			optBtn.MouseButton1Click:Connect(function()
				selected = option
				dropButton.Text = selected
				isOpen = false
				dropFrame.Visible = false
				if callback then
					callback(selected)
				end
			end)
		end
		dropFrame.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y)
	end

	RefreshOptions(options)

	dropButton.MouseButton1Click:Connect(function()
		isOpen = not isOpen
		if isOpen then
			-- Maksimum 130 pixel yükseklik (veya kaç öğe varsa o kadar)
			local count = 0
			for _, c in pairs(dropFrame:GetChildren()) do
				if c:IsA("TextButton") then count += 1 end
			end
			local height = math.min(count * 26, 130) 
			
			dropFrame.Visible = true
			dropFrame:TweenSize(UDim2.new(0.48, 0, 0, height), "Out", "Quad", 0.2, true)
			container.ZIndex = 10 -- Açıldığında container'ı da öne çıkar
		else
			dropFrame:TweenSize(UDim2.new(0.48, 0, 0, 0), "Out", "Quad", 0.2, true, function()
				dropFrame.Visible = false
				container.ZIndex = 5
			end)
		end
	end)

	return {
		SetOptions = function(newOptions)
			RefreshOptions(newOptions)
		end,
		Get = function()
			return selected
		end,
		Set = function(value)
			selected = value
			dropButton.Text = value
		end
	}
end

function UILibrary:CreateSlider(tab, text, min, max, default, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 50)
	container.BackgroundColor3 = Theme.Element
	container.BackgroundTransparency = 0.25
	container.BorderSizePixel = 0
	container.ZIndex = 3
	container.Parent = tab.Content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -20, 0, 20)
	label.Position = UDim2.new(0, 12, 0, 4)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Theme.Text
	label.Font = Enum.Font.Gotham
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 4
	label.Parent = container

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 50, 0, 20)
	valueLabel.Position = UDim2.new(1, -58, 0, 4)
	valueLabel.BackgroundTransparency = 1
	valueLabel.Text = tostring(default)
	valueLabel.TextColor3 = Theme.Accent
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 13
	valueLabel.TextXAlignment = Enum.TextXAlignment.Right
	valueLabel.ZIndex = 4
	valueLabel.Parent = container

	local sliderBack = Instance.new("Frame")
	sliderBack.Size = UDim2.new(1, -24, 0, 6)
	sliderBack.Position = UDim2.new(0, 12, 0, 32)
	sliderBack.BackgroundColor3 = Color3.fromRGB(40, 45, 65)
	sliderBack.BorderSizePixel = 0
	sliderBack.ZIndex = 4
	sliderBack.Parent = container

	local backCorner = Instance.new("UICorner")
	backCorner.CornerRadius = UDim.new(1, 0)
	backCorner.Parent = sliderBack

	local sliderFill = Instance.new("Frame")
	sliderFill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	sliderFill.BackgroundColor3 = Theme.Accent
	sliderFill.BorderSizePixel = 0
	sliderFill.ZIndex = 5
	sliderFill.Parent = sliderBack

	local fillCorner = Instance.new("UICorner")
	fillCorner.CornerRadius = UDim.new(1, 0)
	fillCorner.Parent = sliderFill

	local sliding = false

	local function update(input)
		local pos = math.clamp((input.Position.X - sliderBack.AbsolutePosition.X) / sliderBack.AbsoluteSize.X, 0, 1)
		local value = math.floor((min + (max - min) * pos) * 10) / 10

		sliderFill.Size = UDim2.new(pos, 0, 1, 0)
		valueLabel.Text = tostring(value)

		if callback then
			callback(value)
		end
	end

	sliderBack.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			sliding = true
			update(input)
		end
	end)

	sliderBack.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			sliding = false
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if sliding and input.UserInputType == Enum.UserInputType.MouseMovement then
			update(input)
		end
	end)
end

function UILibrary:CreateColorPicker(tab, text, default, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 36)
	container.BackgroundColor3 = Theme.Element
	container.BackgroundTransparency = 0.25
	container.BorderSizePixel = 0
	container.ZIndex = 3
	container.ClipsDescendants = true -- RGB paneli için
	container.Parent = tab.Content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.6, 0, 0, 36)
	label.Position = UDim2.new(0, 12, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Theme.Text
	label.Font = Enum.Font.Gotham
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 4
	label.Parent = container

	local colorDisplay = Instance.new("TextButton")
	colorDisplay.Size = UDim2.new(0, 40, 0, 20)
	colorDisplay.Position = UDim2.new(1, -50, 0, 8)
	colorDisplay.BackgroundColor3 = default or Color3.fromRGB(255, 255, 255)
	colorDisplay.Text = ""
	colorDisplay.ZIndex = 4
	colorDisplay.Parent = container

	local dispCorner = Instance.new("UICorner")
	dispCorner.CornerRadius = UDim.new(0, 4)
	dispCorner.Parent = colorDisplay

	-- RGB Paneli (Kaydırdıkça açılan kısım)
	local pickerFrame = Instance.new("Frame")
	pickerFrame.Size = UDim2.new(1, -24, 0, 100)
	pickerFrame.Position = UDim2.new(0, 12, 0, 40)
	pickerFrame.BackgroundTransparency = 1
	pickerFrame.ZIndex = 4
	pickerFrame.Parent = container

	local r, g, b = default.R * 255, default.G * 255, default.B * 255

	local function updateColor()
		local newColor = Color3.fromRGB(r, g, b)
		colorDisplay.BackgroundColor3 = newColor
		if callback then callback(newColor) end
	end

	-- Basit RGB Kaydırıcıları Oluşturma
	local function createRGBSlider(name, color, pos, defaultVal)
		local sLabel = Instance.new("TextLabel")
		sLabel.Size = UDim2.new(1, 0, 0, 20)
		sLabel.Position = UDim2.new(0, 0, 0, pos)
		sLabel.Text = name .. ": " .. math.floor(defaultVal)
		sLabel.TextColor3 = color
		sLabel.BackgroundTransparency = 1
		sLabel.Font = Enum.Font.Gotham
		sLabel.TextSize = 10
		sLabel.TextXAlignment = Enum.TextXAlignment.Left
		sLabel.Parent = pickerFrame

		local bar = Instance.new("TextButton")
		bar.Size = UDim2.new(1, 0, 0, 4)
		bar.Position = UDim2.new(0, 0, 0, pos + 20)
		bar.BackgroundColor3 = Color3.fromRGB(40, 45, 65)
		bar.Text = ""
		bar.Parent = pickerFrame

		local fill = Instance.new("Frame")
		fill.Size = UDim2.new(defaultVal / 255, 0, 1, 0)
		fill.BackgroundColor3 = color
		fill.Parent = bar

		local sliding = false
		local function update(input)
			local move = math.clamp((input.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X, 0, 1)
			fill.Size = UDim2.new(move, 0, 1, 0)
			local val = math.floor(move * 255)
			sLabel.Text = name .. ": " .. val
			if name == "R" then r = val elseif name == "G" then g = val else b = val end
			updateColor()
		end

		bar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then sliding = true update(input) end end)
		UserInputService.InputChanged:Connect(function(input) if sliding and input.UserInputType == Enum.UserInputType.MouseMovement then update(input) end end)
		UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then sliding = false end end)
	end

	createRGBSlider("R", Color3.fromRGB(255, 100, 100), 0, r)
	createRGBSlider("G", Color3.fromRGB(100, 255, 100), 30, g)
	createRGBSlider("B", Color3.fromRGB(100, 100, 255), 60, b)

	-- Tıklayınca Genişleme Animasyonu
	local expanded = false
	colorDisplay.MouseButton1Click:Connect(function()
		expanded = not expanded
		TweenService:Create(container, TweenInfo.new(0.3), {Size = expanded and UDim2.new(1, 0, 0, 150) or UDim2.new(1, 0, 0, 36)}):Play()
	end)
end

function UILibrary:CreateInput(tab, text, placeholder, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 45)
	container.BackgroundColor3 = Theme.Element
	container.BackgroundTransparency = 0.25
	container.BorderSizePixel = 0
	container.ZIndex = 3
	container.Parent = tab.Content

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 8)
	corner.Parent = container

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.5, 0, 1, 0)
	label.Position = UDim2.new(0, 12, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Theme.Text
	label.Font = Enum.Font.Gotham
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 4
	label.Parent = container

	local inputFrame = Instance.new("Frame")
	inputFrame.Size = UDim2.new(0.4, 0, 0, 26)
	inputFrame.Position = UDim2.new(0.55, 0, 0.5, -13)
	inputFrame.BackgroundColor3 = Color3.fromRGB(35, 40, 60)
	inputFrame.ZIndex = 4
	inputFrame.Parent = container

	local inputCorner = Instance.new("UICorner")
	inputCorner.CornerRadius = UDim.new(0, 6)
	inputCorner.Parent = inputFrame

	local textBox = Instance.new("TextBox")
	textBox.Size = UDim2.new(1, -10, 1, 0)
	textBox.Position = UDim2.new(0, 5, 0, 0)
	textBox.BackgroundTransparency = 1
	textBox.Text = ""
	textBox.PlaceholderText = placeholder or "ID Gir..."
	textBox.TextColor3 = Theme.Text
	textBox.PlaceholderColor3 = Theme.TextDim
	textBox.Font = Enum.Font.Gotham
	textBox.TextSize = 12
	textBox.ClearTextOnFocus = false
	textBox.ZIndex = 5
	textBox.Parent = inputFrame

	-- Yazı yazılıp Enter'a basıldığında çalışır
	textBox.FocusLost:Connect(function(enterPressed)
		if enterPressed then
			if callback then
				callback(textBox.Text)
			end
		end
	end)
end

-------------------------------------------------
-- KULLANIM
-------------------------------------------------

local MapNames = {
	"Hospital3", "Office3", "House2", "Hotel", "Mansion", 
	"Factory", "Bank", "School", "Lab", "ResearchFacility", 
	"BioLab", "BeachResort", "Pier", "Workplace", "Mansion2", "Yacht", "MilBase", "PoliceStation"
}


local function GetActiveMap()
	for _, name in pairs(MapNames) do
		local map = workspace:FindFirstChild(name)
		if map then return map end
	end
	return nil
end


local Window = UILibrary.new("2btr Hub")

local MainTab = Window:CreateTab("Main", "🏠")
local PlayerTab = Window:CreateTab("Player", "👤")
local CombatTab = Window:CreateTab("Combat", "⚔")
local TrollTab = Window:CreateTab("Troll", "🎭")
local FarmTab = Window:CreateTab("Farm", "💰")
local AnimationTab = Window:CreateTab("Animation", "🏃‍♂️")
local TeleportTab = Window:CreateTab("Teleport", "📍")
local ExtraTab = Window:CreateTab("Extra", "✨")

Window:CreateSection(ExtraTab, "Lighting & Atmosphere")

-- Ambient (Genel Işık Rengi)
Window:CreateColorPicker(ExtraTab, "Ambient Color", game.Lighting.Ambient, function(color)
    game.Lighting.Ambient = color
    game.Lighting.OutdoorAmbient = color
end)

-- Fog (Sis Rengi)
Window:CreateColorPicker(ExtraTab, "Fog Color", game.Lighting.FogColor, function(color)
    game.Lighting.FogColor = color
end)

-- Sis Yoğunluğu
Window:CreateSlider(ExtraTab, "Fog Density (End)", 100, 5000, 5000, function(value)
    game.Lighting.FogEnd = value
end)

Window:CreateSection(ExtraTab, "Brightness Settings")

-- Parlaklık
Window:CreateSlider(ExtraTab, "Brightness", 0, 10, 2, function(value)
    game.Lighting.Brightness = value
end)

-- Saat Ayarı
Window:CreateSlider(ExtraTab, "Time of Day", 0, 24, 14, function(value)
    game.Lighting.ClockTime = value
end)

Window:CreateSection(ExtraTab, "World Effects")

-- Shadows (Gölgeler)
Window:CreateToggle(ExtraTab, "Global Shadows", game.Lighting.GlobalShadows, function(state)
    game.Lighting.GlobalShadows = state
end)





local SelectedTPPlayer = "None"

-- Işınlanma Fonksiyonu
local function TeleportTo(cframe)
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = cframe
    end
end

-------------------------------------------------
-- TELEPORT SEKİMESİ İÇERİĞİ
-------------------------------------------------

Window:CreateSection(TeleportTab, "World Teleport")

-- MM2 Lobby Koordinatları (Genel koordinat)
Window:CreateToggle(TeleportTab, "Teleport to Lobby", false, function(state)
    if state then
        TeleportTo(CFrame.new(-8.67327880859375, -66.89867401123047, -4.764404296875)) -- MM2 Standart Lobi Koordinatı
        -- Toggle'ı geri kapatmak için (isteğe bağlı)
    end
end)

Window:CreateSection(TeleportTab, "Player Teleport")

-- Oyuncu Seçme Dropdown
local TPPlayerDropdown = Window:CreateDropdown(TeleportTab, "Select Player", {"None"}, "None", function(selected)
    SelectedTPPlayer = selected
end)

-- Oyuncu Listesini Güncelleme (Teleport için ayrı döngü)
task.spawn(function()
    while task.wait(3) do
        local list = {"None"}
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= game.Players.LocalPlayer then 
                table.insert(list, p.Name) 
            end
        end
        if TPPlayerDropdown then 
            TPPlayerDropdown.SetOptions(list) 
        end
    end
end)

-- Seçili Oyuncuya Işınlanma
Window:CreateToggle(TeleportTab, "Teleport to Selected", false, function(state)
    if state and SelectedTPPlayer ~= "None" then
        local target = game.Players:FindFirstChild(SelectedTPPlayer)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            TeleportTo(target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 3, 0)) -- Tepesine ışınlanmak için +3 birim
        else
            warn("Oyuncu bulunamadı veya karakteri yüklenmemiş.")
        end
    end
end)

Window:CreateSection(TeleportTab, "Settings")
Window:CreateToggle(TeleportTab, "Keep TP (Auto TP)", false, function(state)
    _G.AutoTP = state
    while _G.AutoTP do
        if SelectedTPPlayer ~= "None" then
            local target = game.Players:FindFirstChild(SelectedTPPlayer)
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                TeleportTo(target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 3, 0))
            end
        end
        task.wait(0.1)
    end
end)






-------------------------------------------------
-- 1. ÖNCE TABLO TANIMLANMALI (En Üstte Olmalı)
-------------------------------------------------
local Packs = {
    Ninja = {
        idle = {656117400, 656118341},
        walk = 656121766,
        run = 656118852,
        jump = 656117878,
        fall = 656115606,
        climb = 656114359
    },
    Mage = {
        idle = {707742142, 707855907},
        walk = 707897309,
        run = 707861613,
        jump = 707853694,
        fall = 707829716,
        climb = 707826056
    },
    Levitation = {
        idle = {616006778, 616008087},
        walk = 616013216,
        run = 616010382,
        jump = 616008936,
        fall = 616005863,
        climb = 616003713
    },
    Cartoony = {
        idle = {742637544, 742638445},
        walk = 742640026,
        run = 742638842,
        jump = 742637942,
        fall = 742637151,
        climb = 742636889
    },
    Knight = {
        idle = {657595757, 657568135},
        walk = 657552124,
        run = 657564596,
        jump = 658409194,
        fall = 657600338,
        climb = 658360781
    },
    Robot = {
        idle = {616088211, 616089559},
        walk = 616095330,
        run = 616091570,
        jump = 616090535,
        fall = 616087089,
        climb = 616086039
    }
}

local currentSelectedPack = nil

-------------------------------------------------
-- 2. SONRA FONKSİYONLAR GELMELİ
-------------------------------------------------
local function SetAnimationPack(packData)
    if not packData then return end
    local char = game.Players.LocalPlayer.Character
    if not char then return end
    
    local animate = char:FindFirstChild("Animate")
    if not animate then return end

    pcall(function()
        if packData.idle then
            animate.idle.Animation1.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.idle[1]
            animate.idle.Animation2.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.idle[2]
        end
        if packData.walk then
            animate.walk.WalkAnim.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.walk
        end
        if packData.run then
            animate.run.RunAnim.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.run
        end
        if packData.jump then
            animate.jump.JumpAnim.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.jump
        end
        if packData.fall then
            animate.fall.FallAnim.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.fall
        end
        if packData.climb and animate:FindFirstChild("climb") then
            animate.climb.ClimbAnim.AnimationId = "http://www.roblox.com/asset/?id=" .. packData.climb
        end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.Jump = true end
    end)
end

-- Karakter ölünce tekrar yükleme takibi
game.Players.LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.7)
    if currentSelectedPack then
        SetAnimationPack(currentSelectedPack)
    end
end)

-------------------------------------------------
-- 3. EN SON UI BUTONLARI OLUŞTURULMALI
-------------------------------------------------
Window:CreateSection(AnimationTab, "Animation Packs")

Window:CreateToggle(AnimationTab, "Ninja Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Ninja 
        SetAnimationPack(Packs.Ninja)
    else
        currentSelectedPack = nil
    end
end)

Window:CreateToggle(AnimationTab, "Mage Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Mage 
        SetAnimationPack(Packs.Mage)
    else
        currentSelectedPack = nil
    end
end)

Window:CreateToggle(AnimationTab, "Levitation Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Levitation 
        SetAnimationPack(Packs.Levitation)
    else
        currentSelectedPack = nil
    end
end)

Window:CreateToggle(AnimationTab, "Cartoony Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Cartoony 
        SetAnimationPack(Packs.Cartoony)
    else
        currentSelectedPack = nil
    end
end)

Window:CreateToggle(AnimationTab, "Knight Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Knight 
        SetAnimationPack(Packs.Knight)
    else
        currentSelectedPack = nil
    end
end)

Window:CreateToggle(AnimationTab, "Robot Animation", false, function(state)
    if state then 
        currentSelectedPack = Packs.Robot 
        SetAnimationPack(Packs.Robot)
    else
        currentSelectedPack = nil
    end
end)




local AutoFarmEnabled = false
local FarmSpeed = 30 -- Varsayılan hız
local currentFarmTween = nil

-------------------------------------------------
-- FARM YARDIMCI FONKSİYONLARI
-------------------------------------------------


local function GetNearestCoin()
	local map = GetActiveMap()
	if not map then return nil end
	
	local container = map:FindFirstChild("CoinContainer")
	if not container then return nil end
	
	local character = LocalPlayer.Character
	if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
	
	local nearestCoin = nil
	local shortestDistance = math.huge
	
	for _, coin in pairs(container:GetChildren()) do
		if coin.Name == "Coin_Server" and coin:IsA("Part") then
			local distance = (character.HumanoidRootPart.Position - coin.Position).Magnitude
			if distance < shortestDistance then
				shortestDistance = distance
				nearestCoin = coin
			end
		end
	end
	
	return nearestCoin
end

-- Farm Döngüsü
-- Farm Döngüsü
task.spawn(function()
	while task.wait(0.1) do
	    local isAlive = LocalPlayer:GetAttribute("Alive") == true

		if AutoFarmEnabled and isAlive then
			local character = LocalPlayer.Character
			if not character or not character:FindFirstChild("HumanoidRootPart") then continue end
			
			if currentFarmTween then continue end

			local targetCoin = GetNearestCoin()
			
			if targetCoin and targetCoin.Parent then
				local root = character.HumanoidRootPart
				local distance = (root.Position - targetCoin.Position).Magnitude
				local duration = distance / FarmSpeed
				
				character.Humanoid.PlatformStand = true 
				
				currentFarmTween = TweenService:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {
					CFrame = targetCoin.CFrame + Vector3.new(0, 1, 0)
				})
				
				currentFarmTween:Play()
				
				local connection
				connection = targetCoin.AncestryChanged:Connect(function()
					if not targetCoin:IsDescendantOf(workspace) and currentFarmTween then
						currentFarmTween:Cancel()
					end
				end)
				
				currentFarmTween.Completed:Wait()
				
				-- ==========================================
				-- ==========================================
				if targetCoin and targetCoin.Parent then
					targetCoin:Destroy()
				end
				-- ==========================================

				if connection then connection:Disconnect() end
				currentFarmTween = nil
				character.Humanoid.PlatformStand = false
			end
		else
			if currentFarmTween then
				currentFarmTween:Cancel()
				currentFarmTween = nil
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
					LocalPlayer.Character.Humanoid.PlatformStand = false
				end
			end
		end
	end
end)

Window:CreateSection(FarmTab, "Auto Coin Farm")

Window:CreateToggle(FarmTab, "Auto Collect Coins", false, function(state)
	AutoFarmEnabled = state
	if not state and currentFarmTween then
		currentFarmTween:Cancel()
		currentFarmTween = nil
	end
end)

Window:CreateSlider(FarmTab, "Farm Speed", 10, 100, 30, function(value)
	FarmSpeed = value
end)

local AntiFlingEnabled = false
local FlingTargetEnabled = false
local FlingMurderer = false
local FlingSheriff = false
local SelectedPlayer = "None"

-- 1. Anti-Fling Sistemi (Geliştirilmiş ve Fling ile uyumlu)
game:GetService("RunService").Stepped:Connect(function()
    if not AntiFlingEnabled then return end
    
    -- EĞER FLING AÇIKSA ANTİ-FLING DURMALI (Çakışmayı önlemek için)
    if FlingMurderer or FlingSheriff or FlingTargetEnabled then return end

    local char = game.Players.LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root and root.Velocity.Magnitude > 150 then
            root.Velocity = Vector3.zero
            root.RotVelocity = Vector3.zero
        end
        
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= game.Players.LocalPlayer and p.Character then
                for _, part in pairs(p.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false -- Sadece fling kapalıyken içinden geçer
                        part.Velocity = Vector3.new(0, 0, 0)
                    end
                end
            end
        end
    end
end)

-- 2. Rol Bulma Fonksiyonları (Çanta/Backpack kontrolü eklendi)
local function GetMurderer()
    for _, p in pairs(game.Players:GetPlayers()) do
        if p.Character and (p.Character:FindFirstChild("Knife") or p.Backpack:FindFirstChild("Knife")) then 
            return p 
        end
    end
    return nil
end

local function GetSheriff()
    for _, p in pairs(game.Players:GetPlayers()) do
        if p.Character and (p.Character:FindFirstChild("Gun") or p.Backpack:FindFirstChild("Gun") or p.Character:FindFirstChild("Revolver") or p.Backpack:FindFirstChild("Revolver")) then 
            return p 
        end
    end
    return nil
end

-- 3. Ana Fling Döngüsü (Güçlendirilmiş)
game:GetService("RunService").Heartbeat:Connect(function()
    local target = nil
    
    if FlingMurderer then 
        target = GetMurderer()
    elseif FlingSheriff then 
        target = GetSheriff()
    elseif FlingTargetEnabled and SelectedPlayer ~= "None" then 
        target = game.Players:FindFirstChild(SelectedPlayer) 
    end

    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local lroot = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local lhum = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        local troot = target.Character.HumanoidRootPart
        
        if lroot and lhum then
            -- Fling sırasında çarpışmayı aç ki çarptığında fırlasınlar
            for _, part in pairs(game.Players.LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = true end
            end
            
            -- Hedefin içine gir ve dön
            lroot.CFrame = troot.CFrame * CFrame.Angles(0, math.rad(math.random(0,360)), 0)
            lroot.Velocity = Vector3.new(8000, 8000, 8000)
            lroot.RotVelocity = Vector3.new(8000, 8000, 8000)
        end
    end
end)

-- 4. UI Elemanları
Window:CreateSection(TrollTab, "Protection")
Window:CreateToggle(TrollTab, "Anti-Fling", false, function(state)
    AntiFlingEnabled = state
end)

Window:CreateToggle(TrollTab, "Fling Murderer", false, function(state)
    FlingMurderer = state
end)
Window:CreateToggle(TrollTab, "Fling Sheriff", false, function(state)
    FlingSheriff = state
end)

local PlayerDropdown = Window:CreateDropdown(TrollTab, "Select Player", {"None"}, "None", function(selected)
    SelectedPlayer = selected
end)

task.spawn(function()
    while task.wait(3) do
        local list = {"None"}
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= game.Players.LocalPlayer then table.insert(list, p.Name) end
        end
        if PlayerDropdown then PlayerDropdown.SetOptions(list) end
    end
end)

Window:CreateToggle(TrollTab, "Fling Target", false, function(state)
    FlingTargetEnabled = state
    if not state then
        local lroot = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if lroot then 
            lroot.Velocity = Vector3.zero
            lroot.RotVelocity = Vector3.zero
        end
    end
end)





-- Hitbox Değişkenleri
local HitboxEnabled = false
local HitboxSize = 10

-- Auto Grab Gun Değişkenleri
local AutoGrabGunEnabled = false
local GrabConnection = nil
local isGrabbing = false

-- Auto Kill Değişkenleri
local AutoKillEnabled = false
local AutoKillConnection = nil

-------------------------------------------------
-- YARDIMCI FONKSİYONLAR
-------------------------------------------------

local function FindGunDrop()
	for _, mapName in pairs(MapNames) do
		local map = workspace:FindFirstChild(mapName)
		if map then
			local gunDrop = map:FindFirstChild("GunDrop")
			if gunDrop then return gunDrop end
		end
	end
	return nil
end

local function CanGrabGun()
	local character = LocalPlayer.Character
	local backpack = LocalPlayer:FindFirstChild("Backpack")
	if (character and character:FindFirstChild("Knife")) or (backpack and backpack:FindFirstChild("Knife")) then
		return false
	end
	if LocalPlayer:GetAttribute("Alive") ~= true then return false end
	return true
end

local function GrabGun()
	if isGrabbing or not CanGrabGun() then return end
	isGrabbing = true
	local gunDrop = FindGunDrop()
	local character = LocalPlayer.Character
	if gunDrop and character and character:FindFirstChild("HumanoidRootPart") then
		local root = character.HumanoidRootPart
		local oldCFrame = root.CFrame
		root.CFrame = gunDrop.CFrame + Vector3.new(0, 3, 0)
		task.wait(0.2)
		root.CFrame = oldCFrame
	end
	task.wait(0.6)
	isGrabbing = false
end

-- Bıçağı bulma ve kuşanma fonksiyonu
local function GetKnife()
	local character = LocalPlayer.Character
	if not character then return nil end
	
	local knife = character:FindFirstChild("Knife")
	if knife then return knife end
	
	local backpack = LocalPlayer:FindFirstChild("Backpack")
	knife = backpack and backpack:FindFirstChild("Knife")
	if knife then
		character:FindFirstChildOfClass("Humanoid"):EquipTool(knife)
		return knife
	end
	return nil
end

-------------------------------------------------
-- COMBAT TAB İÇERİK
-------------------------------------------------

-- HITBOX SECTION
Window:CreateSection(CombatTab, "Hitbox")

Window:CreateToggle(CombatTab, "Hitbox Expander", false, function(state)
	HitboxEnabled = state
	if not state then
		for _, player in pairs(game:GetService("Players"):GetPlayers()) do
			if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
				local root = player.Character.HumanoidRootPart
				root.Size = Vector3.new(2, 2, 1)
				root.Transparency = 1
				root.CanCollide = true
			end
		end
	end
end)

Window:CreateSlider(CombatTab, "Hitbox Size", 2, 50, 10, function(value)
	HitboxSize = value
end)

-- AUTO GRAB SECTION
Window:CreateSection(CombatTab, "Auto Grab")

Window:CreateToggle(CombatTab, "Auto Grab Gun", false, function(state)
	AutoGrabGunEnabled = state
	if state then
		if GrabConnection then GrabConnection:Disconnect() end
		GrabConnection = game:GetService("RunService").Heartbeat:Connect(function()
			if AutoGrabGunEnabled and not isGrabbing and CanGrabGun() and FindGunDrop() then
				GrabGun()
			end
		end)
	elseif GrabConnection then
		GrabConnection:Disconnect()
		GrabConnection = nil
	end
end)

-- AUTO KILL SECTION (YENİ)
Window:CreateSection(CombatTab, "Auto Kill")

Window:CreateToggle(CombatTab, "Auto Kill (Murderer Only)", false, function(state)
	AutoKillEnabled = state
	
	if state then
		AutoKillConnection = game:GetService("RunService").Heartbeat:Connect(function()
			if not AutoKillEnabled then return end
			
			local knife = GetKnife()
			if not knife then return end -- Bıçak yoksa çalışma
			
			for _, player in pairs(game:GetService("Players"):GetPlayers()) do
				if player ~= LocalPlayer and player.Character and player:GetAttribute("Alive") == true then
					local targetRoot = player.Character:FindFirstChild("HumanoidRootPart")
					local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
					
					if targetRoot and myRoot then
						-- Oyuncunun arkasına ışınlan
						myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 1.5)
						
						-- Bıçağı salla
						knife:Activate() 
						
						-- Bir sonraki oyuncuya geçmeden önce çok kısa bekle (Opsiyonel)
						task.wait(0.1)
					end
				end
			end
		end)
	else
		if AutoKillConnection then
			AutoKillConnection:Disconnect()
			AutoKillConnection = nil
		end
	end
end)

-- Hitbox Güncelleme Döngüsü
game:GetService("RunService").RenderStepped:Connect(function()
	if not HitboxEnabled then return end
	for _, player in pairs(game:GetService("Players"):GetPlayers()) do
		if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			local root = player.Character.HumanoidRootPart
			root.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
			root.Transparency = 0.7
			root.Color = Color3.fromRGB(255, 0, 0)
			root.CanCollide = false
		end
	end
end)







-- ESP Ayarları
local ESPEnabled = false
local MurderESPEnabled = false
local SheriffESPEnabled = false
local ShowNames = true
local ChamsEnabled = false

-- YENİ AYARLAR (CHAMS DESTEKLİ)
local GunESPEnabled = false
local CoinESPEnabled = false

-- Kalıcı rol hafızası
local MurderList = {}
local SheriffList = {}

local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "ESPFolder"
ESPFolder.Parent = game:GetService("CoreGui")

-------------------------------------------------
-- YARDIMCI FONKSİYONLAR
-------------------------------------------------

local function GetActiveMap()
	for _, name in pairs(MapNames) do
		local map = workspace:FindFirstChild(name)
		if map then return map end
	end
	return nil
end

local function CreateWorldChams(obj, color, nameText)
	if not obj or not obj:IsA("BasePart") then return end
	local id = obj:GetDebugId()
	if ESPFolder:FindFirstChild(id) then return end

	-- Chams (Highlight)
	local highlight = Instance.new("Highlight")
	highlight.Name = id
	highlight.Adornee = obj
	highlight.FillColor = color
	highlight.OutlineColor = Color3.new(1, 1, 1)
	highlight.FillTransparency = 0.4
	highlight.OutlineTransparency = 0
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.Parent = ESPFolder

	-- Billboard (Üstünde Yazı)
	local boxGui = Instance.new("BillboardGui")
	boxGui.Name = id .. "_Label"
	boxGui.Adornee = obj
	boxGui.Size = UDim2.new(0, 100, 0, 40)
	boxGui.StudsOffset = Vector3.new(0, 1.5, 0)
	boxGui.AlwaysOnTop = true
	boxGui.Parent = ESPFolder

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = nameText
	label.TextColor3 = color
	label.TextStrokeTransparency = 0
	label.Font = Enum.Font.GothamBold
	label.TextSize = 14
	label.Parent = boxGui

	-- MM2'ye özel temizleme kontrolü: Obje silinirse veya haritadan uzaklaşırsa temizle
	task.spawn(function()
		while obj and obj:IsDescendantOf(workspace) and obj.Parent ~= nil do
			task.wait(0.5)
			-- Eğer ESP kategorisi kapatılırsa hemen sil
			if (nameText == "Gun" and not GunESPEnabled) or (nameText == "Coin" and not CoinESPEnabled) then
				break
			end
		end
		highlight:Destroy()
		boxGui:Destroy()
	end)
end

local function RemoveESP(player)
	local box = ESPFolder:FindFirstChild(player.Name)
	if box then box:Destroy() end

	local name = ESPFolder:FindFirstChild(player.Name .. "_Name")
	if name then name:Destroy() end

	local chams = ESPFolder:FindFirstChild(player.Name .. "_Chams")
	if chams then chams:Destroy() end
end

local function GetESPColor(player)
	if MurderESPEnabled and MurderList[player] then
		return Color3.fromRGB(255, 50, 50)
	end
	if SheriffESPEnabled and SheriffList[player] then
		return Color3.fromRGB(50, 150, 255)
	end
	return Color3.fromRGB(160, 160, 160)
end

-------------------------------------------------
-- ANA ESP MANTIĞI
-------------------------------------------------

local function CreateESP(player)
	if player == LocalPlayer then return end
	local character = player.Character
	if not character then return end

	local root = character:FindFirstChild("HumanoidRootPart")
	if not root then return end

	RemoveESP(player)
	local color = GetESPColor(player)

	if ChamsEnabled then
		local highlight = Instance.new("Highlight")
		highlight.Name = player.Name .. "_Chams"
		highlight.Adornee = character
		highlight.FillColor = color
		highlight.OutlineColor = color
		highlight.FillTransparency = 0.5
		highlight.Parent = ESPFolder
	else
		local boxGui = Instance.new("BillboardGui")
		boxGui.Name = player.Name
		boxGui.Adornee = root
		boxGui.Size = UDim2.new(4, 0, 6, 0)
		boxGui.AlwaysOnTop = true
		boxGui.Parent = ESPFolder

		local boxFrame = Instance.new("Frame")
		boxFrame.Size = UDim2.new(1, 0, 1, 0)
		boxFrame.BackgroundTransparency = 1
		boxFrame.Parent = boxGui

		local stroke = Instance.new("UIStroke")
		stroke.Thickness = 1.5
		stroke.Color = color
		stroke.Parent = boxFrame
	end

	if ShowNames then
		local nameGui = Instance.new("BillboardGui")
		nameGui.Name = player.Name .. "_Name"
		nameGui.Adornee = root
		nameGui.Size = UDim2.new(0, 200, 0, 30)
		nameGui.StudsOffset = Vector3.new(0, 3.8, 0)
		nameGui.AlwaysOnTop = true
		nameGui.Parent = ESPFolder

		local nameLabel = Instance.new("TextLabel")
		nameLabel.Size = UDim2.new(1, 0, 1, 0)
		nameLabel.BackgroundTransparency = 1
		nameLabel.Text = player.Name
		nameLabel.TextColor3 = color
		nameLabel.Font = Enum.Font.GothamBold
		nameLabel.TextSize = 13
		nameLabel.Parent = nameGui
	end
end

local function ClearAllESP()
	ESPFolder:ClearAllChildren()
end

local function RefreshAllESP()
	ClearAllESP()
	if ESPEnabled then
		for _, player in pairs(Players:GetPlayers()) do
			if player.Character then CreateESP(player) end
		end
	end
end

task.spawn(function()
	while task.wait(1) do
		if not CoinESPEnabled and not GunESPEnabled then 
			continue 
		end
		
		local map = GetActiveMap()
		if map then
			-- Gun Drop Kontrolü
			if GunESPEnabled then
				local gun = map:FindFirstChild("GunDrop")
				if gun and gun:IsA("BasePart") then
					CreateWorldChams(gun, Color3.fromRGB(0, 255, 255), "Gun")
				end
			end

			-- Coin Kontrolü
			if CoinESPEnabled then
				local container = map:FindFirstChild("CoinContainer")
				if container then
					for _, coin in pairs(container:GetChildren()) do
						if coin.Name == "Coin_Server" and coin:IsA("BasePart") then
							CreateWorldChams(coin, Color3.fromRGB(255, 215, 0), "Coin")
						end
					end
				end
			end
		end
	end
end)

-------------------------------------------------
-- TAKİP VE DÖNGÜLER
-------------------------------------------------

local function CheckWeapons(player)
	local character = player.Character
	if not character then return end
	if character:FindFirstChild("Knife") then
		MurderList[player] = true
		SheriffList[player] = nil
	elseif character:FindFirstChild("Gun") or character:FindFirstChild("Revolver") then
		SheriffList[player] = true
		MurderList[player] = nil
	end
end

game:GetService("RunService").Heartbeat:Connect(function()
	if not ESPEnabled then return end
	for _, player in pairs(Players:GetPlayers()) do
		if player ~= LocalPlayer and player.Character then
			CheckWeapons(player)
			local color = GetESPColor(player)
			
			local chams = ESPFolder:FindFirstChild(player.Name .. "_Chams")
			if chams then chams.FillColor = color chams.OutlineColor = color end
			
			local boxGui = ESPFolder:FindFirstChild(player.Name)
			if boxGui and boxGui:FindFirstChild("Frame") then 
				local stroke = boxGui.Frame:FindFirstChild("UIStroke")
				if stroke then stroke.Color = color end
			end
		end
	end
end)

-- Karakter Takibi
local function SetupPlayer(player)
	player.CharacterAdded:Connect(function()
		MurderList[player] = nil
		SheriffList[player] = nil
		task.wait(0.5)
		if ESPEnabled then CreateESP(player) end
	end)
	player.CharacterRemoving:Connect(function() RemoveESP(player) end)
end

Players.PlayerAdded:Connect(SetupPlayer)
for _, player in pairs(Players:GetPlayers()) do SetupPlayer(player) end

-------------------------------------------------
-- PLAYER TAB ARAYÜZÜ
-------------------------------------------------

Window:CreateSection(PlayerTab, "Player ESP")

Window:CreateToggle(PlayerTab, "Enable ESP", false, function(state)
	ESPEnabled = state
	if state then RefreshAllESP() else ClearAllESP() end
end)

Window:CreateToggle(PlayerTab, "Chams", false, function(state)
	ChamsEnabled = state
	RefreshAllESP()
end)

Window:CreateToggle(PlayerTab, "Murder/Sheriff Special", false, function(state)
	MurderESPEnabled = state
	SheriffESPEnabled = state
	RefreshAllESP()
end)

Window:CreateSection(PlayerTab, "World ESP")

Window:CreateToggle(PlayerTab, "Gun Drop Chams", false, function(state)
	GunESPEnabled = state
	if not state then ClearAllESP() end
end)

Window:CreateToggle(PlayerTab, "Coin Chams", false, function(state)
	CoinESPEnabled = state
	if not state then ClearAllESP() end
end)
-------------------------------------------------
-- MAIN TAB İÇERİK
-------------------------------------------------

-------------------------------------------------
-- MOVEMENT DEĞİŞKENLERİ
-------------------------------------------------
local SpeedEnabled = false
local SpeedValue = 50

local JumpEnabled = false
local JumpValue = 70

local InfiniteJumpEnabled = false -- Yeni

local FlyEnabled = false
local FlySpeed = 50
local BodyVelocity = nil
local BodyGyro = nil

local SpinBotEnabled = false
local SpinSpeed = 25 -- Dönüş hızı

local NoClipEnabled = false 

-------------------------------------------------
-- HAREKET FONKSİYONLARI
-------------------------------------------------

-- NoClip Döngüsü (Karakter ölünce kapanmaz)
game:GetService("RunService").Stepped:Connect(function()
	if NoClipEnabled then
		local character = LocalPlayer.Character
		if character then
			for _, part in pairs(character:GetDescendants()) do
				if part:IsA("BasePart") and part.CanCollide then
					part.CanCollide = false
				end
			end
		end
	end
end)

-- Infinite Jump Mantığı
game:GetService("UserInputService").JumpRequest:Connect(function()
	if InfiniteJumpEnabled then
		local character = LocalPlayer.Character
		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			end
		end
	end
end)

-- SpinBot Döngüsü
-- SpinBot Döngüsü
game:GetService("RunService").Heartbeat:Connect(function()
	local character = LocalPlayer.Character
	if character and character:FindFirstChild("HumanoidRootPart") and character:FindFirstChildOfClass("Humanoid") then
		local root = character.HumanoidRootPart
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		
		if SpinBotEnabled then
			-- Karakterin hareket yönüne otomatik dönmesini engelle
			if humanoid.AutoRotate then
				humanoid.AutoRotate = false
			end
			-- Dönme işlemi
			root.CFrame = root.CFrame * CFrame.Angles(0, math.rad(SpinSpeed), 0)
		else
			-- SpinBot kapalıysa ve AutoRotate hala kapalıysa geri aç
			if not humanoid.AutoRotate then
				humanoid.AutoRotate = true
			end
		end
	end
end)



local function ApplyStats()
	local character = LocalPlayer.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	-- Speed
	if SpeedEnabled then
		humanoid.WalkSpeed = SpeedValue
	else
		humanoid.WalkSpeed = 16
	end

	-- Jump Power
	if JumpEnabled then
		humanoid.UseJumpPower = true -- Bazı MM2 sürümleri için garantiye alalım
		humanoid.JumpPower = JumpValue
	else
		humanoid.JumpPower = 50
	end
end

local function StartFly()
	local character = LocalPlayer.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	local root = character:FindFirstChild("HumanoidRootPart")
	if not humanoid or not root then return end

	humanoid.PlatformStand = true

	BodyVelocity = Instance.new("BodyVelocity")
	BodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	BodyVelocity.Velocity = Vector3.zero
	BodyVelocity.Parent = root

	BodyGyro = Instance.new("BodyGyro")
	BodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
	BodyGyro.P = 10000
	BodyGyro.Parent = root

	local connection
	connection = game:GetService("RunService").RenderStepped:Connect(function()
		if not FlyEnabled or not character or not character.Parent then
			connection:Disconnect()
			return
		end

		local camera = workspace.CurrentCamera
		local moveDirection = Vector3.zero

		if UserInputService:IsKeyDown(Enum.KeyCode.W) then
			moveDirection = moveDirection + camera.CFrame.LookVector
		end
		if UserInputService:IsKeyDown(Enum.KeyCode.S) then
			moveDirection = moveDirection - camera.CFrame.LookVector
		end
		if UserInputService:IsKeyDown(Enum.KeyCode.A) then
			moveDirection = moveDirection - camera.CFrame.RightVector
		end
		if UserInputService:IsKeyDown(Enum.KeyCode.D) then
			moveDirection = moveDirection + camera.CFrame.RightVector
		end
		if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
			moveDirection = moveDirection + Vector3.new(0, 1, 0)
		end
		if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
			moveDirection = moveDirection + Vector3.new(0, -1, 0)
		end

		if moveDirection.Magnitude > 0 then
			moveDirection = moveDirection.Unit * FlySpeed
		end

		BodyVelocity.Velocity = moveDirection
		BodyGyro.CFrame = camera.CFrame
	end)
end

local function StopFly()
	local character = LocalPlayer.Character
	if character then
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid.PlatformStand = false
		end
	end

	if BodyVelocity then BodyVelocity:Destroy() BodyVelocity = nil end
	if BodyGyro then BodyGyro:Destroy() BodyGyro = nil end
end

-- Karakter Yenilendiğinde Ayarları Koru
LocalPlayer.CharacterAdded:Connect(function()
	task.wait(0.5)
	if FlyEnabled then StartFly() else StopFly() end
	ApplyStats()
	-- NoClipEnabled = false (Bu satırı sildik, artık persistenti)
end)

-------------------------------------------------
-- MAIN TAB ARAYÜZ ELEMANLARI
-------------------------------------------------


Window:CreateToggle(MainTab, "Speed (Enable)", false, function(state)
	SpeedEnabled = state
	ApplyStats()
end)

Window:CreateSlider(MainTab, "Speed Value", 16, 200, 50, function(value)
	SpeedValue = value
	if SpeedEnabled then ApplyStats() end
end)

Window:CreateToggle(MainTab, "JumpPower (Enable)", false, function(state)
	JumpEnabled = state
	ApplyStats()
end)

Window:CreateSlider(MainTab, "JumpPower Value", 50, 200, 70, function(value)
	JumpValue = value
	if JumpEnabled then ApplyStats() end
end)

Window:CreateToggle(MainTab, "Infinite Jump", false, function(state)
	InfiniteJumpEnabled = state
end)


Window:CreateToggle(MainTab, "Fly (Enable)", false, function(state)
	FlyEnabled = state
	if state then StartFly() else StopFly() end
end)

Window:CreateSlider(MainTab, "Fly Speed", 10, 200, 50, function(value)
	FlySpeed = value
end)

Window:CreateToggle(MainTab, "NoClip (Persistent)", false, function(state)
	NoClipEnabled = state
end)


Window:CreateToggle(MainTab, "SpinBot", false, function(state)
	SpinBotEnabled = state
	
	-- Toggle kapatıldığı an anlık düzeltme (Döngüye ek olarak)
	local character = LocalPlayer.Character
	if character and not state then
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid.AutoRotate = true
		end
	end
end)

Window:CreateSlider(MainTab, "Spin Speed", 1, 100, 25, function(value)
	SpinSpeed = value
end)

-- 4 saniye bekle
task.wait(2)

loadstring(game:HttpGet("https://raw.githubusercontent.com/2btrleans/scr1ipt/refs/heads/main/yessir.txt"))()

local disappearTween = TweenService:Create(
	imageLabel,
	TweenInfo.new(1.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	{
		ImageTransparency = 1,
		Position = UDim2.new(0.5, -150, 0.55, -150)
	}
)

disappearTween:Play()

disappearTween.Completed:Connect(function()
	screenGui:Destroy()
end)
